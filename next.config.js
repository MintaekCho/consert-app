/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "search.pstatic.net",
      "ticketimage.interpark.com",
      "cdnimg.melon.co.kr",
    ],
  },
};

module.exports = nextConfig;
